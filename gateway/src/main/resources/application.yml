server:
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1

spring:
  application:
    name: gateway
  profiles:
    active: dev

logging:
  level:
    root: info

blackIp:
  continueCounts: 100
  timeInterval: 120
  limitTimes: 5
