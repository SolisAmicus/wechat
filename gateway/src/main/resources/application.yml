server:
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1

spring:
  application:
    name: gateway
  profiles:
    active: dev

logging:
  level:
    root: info

blackIp:
  continueCounts: 3
  timeInterval: 20
  limitTimes: 30
